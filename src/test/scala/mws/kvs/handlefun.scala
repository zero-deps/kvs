package mws.kvs

import org.scalatest.FunSuite

/**
 * Functional simple test cases for the various parts of KVS.
 */
class HandlerFun extends FunSuite {
  import scala.pickling._, Defaults._, static._, binary._

  test("pickler test"){
    //assert(1==1)
  }
}
