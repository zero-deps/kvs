syntax = "proto3";

package mws.kvs;

message File {
  // name – unique value inside directory
  string name = 1;
  // count – number of chunks
  int32 count = 2;
  // size - size of file in bytes
  int64 size = 3;
}
