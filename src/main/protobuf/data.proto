syntax = "proto3";

package mws.rng;

message BucketInfo {
  repeated Vec vc = 1;
  repeated bytes keys = 2;
}

message Data {
  bytes key = 1;
  int32 bucket = 2;
  int64 lastModified = 3;
  repeated Vec vc = 4;
  bytes value = 5;
}

message Vec {
  string key = 1;
  int64 value = 2;
}
